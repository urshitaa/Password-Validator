# Password-Validator
A modular Password Strength Checker for an Employee Portal that enforces security rules. Validates length (≥8), at least one uppercase letter, and one digit using loop-based logic. Gives specific feedback (e.g., “Too short,” “Missing a digit”) and keeps prompting until a valid password is entered.
